
services:
  web:
    image: nginx
    container_name: day37-web
    ports:
      - "${PORT}:80"
    restart: always
    environment:
      APP_ENV: "${APP_ENV}"
      COMPANY: "${ABC Pvt Ltd}"
     VERSION: "v2-revert-demo"
